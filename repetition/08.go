package main

import "fmt"

func main() {

	// Ok, lets do this. We talk about arrays. Arrays are fixed length sequences of elements of the same type.
	// Array in Go are not the same as arrays in other languages.

	var a = [5]int{1, 2, 3, 4, 5}
	fmt.Println("a: ", a)
	
}